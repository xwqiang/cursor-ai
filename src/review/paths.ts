/** Written under PROJECT_ROOT after admin code edits; sent as a Telegram document. */
export const CODE_REVIEW_REL = ".cursor-tg-bot/code-review.html";

/** Agent may echo this marker; stripped from the chat reply. */
export const REVIEW_HTML_MARKER = "REVIEW_HTML:";

/** Agent may echo this marker to request sending an attachment; stripped from chat reply. */
export const ATTACH_FILE_MARKER = "ATTACH_FILE:";
